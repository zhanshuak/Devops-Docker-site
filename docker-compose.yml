version: '3'

services:
  app:
    build: .
    volumes:
      - ./app:/usr/share/nginx/html

  nginx:
    image: nginx:alpine
    ports:
      - "8081:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
